var ReactDOM = require('react-dom');
var React = require('react');
var SmartGrid = require('./components/SpentApp.jsx');

ReactDOM.render(
	<SmartGrid/>,
    document.getElementById('mount-point')
);
